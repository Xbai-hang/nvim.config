column_width = 120          # 每行最大宽度 120 字符
line_endings = "Unix"       # 换行符使用 LF（Unix 风格）
indent_type = "Spaces"      # 使用空格缩进
indent_width = 2            # 每层缩进 2 个空格
quote_style = "AutoPreferDouble" # 字符串优先使用双引号
call_parentheses = "Always" # 函数调用总是加括号
